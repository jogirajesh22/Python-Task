# Create a function count_vowels(word) that counts and returns the number of vowels in a string
def count_vowels(word):
    count=0
    for i in word:
     if i in "aeiouAEIOU":
        count=count+(1)
        return count
result = count_vowels("python")
print(result)    
    
