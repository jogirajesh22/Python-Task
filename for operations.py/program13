#Create a function count_digits(n) that returns the number of digits in a number
def count_digit(n):
    count=0
    for digit in str(n):
        count=count+1

    return count
print(count_digit(129321))
