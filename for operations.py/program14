 #Create a function list_total(numbers) that calculates the total of all elements in a list without using sum().
def list_total(numbers):
    total=0
    for num in numbers:
        total =total+num
    return total
numbers=[10,20,30,40,50]
print(list_total(numbers))    